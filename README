usage: repache.py [-h] -f FILENAME [-u URI] [-m MIMETYPE] [-t THREADS]
                  [-rt REQUEST_TIMEOUT]

optional arguments:
  -h, --help            show this help message and exit
  -f FILENAME, --filename FILENAME
                        path to apache access log file
  -u URI, --uri URI     Base URI to send to. If no URI is given, send requests
                        to URL in parsed line of file
  -m MIMETYPE, --mimetype MIMETYPE
                        send requests of mimetype
  -t THREADS, --threads THREADS
                        Amount of threads to spawn
  -rt REQUEST_TIMEOUT, --request_timeout REQUEST_TIMEOUT
                        Amount of time to wait on server response
